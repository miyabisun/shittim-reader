//! OCR test fixture data (auto-generated placeholder).
//!
//! TODO: Populate with actual fixture data from cell images.

pub const Fixture = struct {
    data: []const u8,
    w: u32,
    h: u32,
    expected: u32,
};

pub const fixtures: []const Fixture = &.{};
pub const number_fixtures: []const Fixture = &.{};
